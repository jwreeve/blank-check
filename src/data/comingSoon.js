// Directors whose series hasn't started yet - App.jsx automatically drops
// any entry here once a matching director shows up in series.js, so this
// only ever needs additions, never manual cleanup.
export const comingSoon = [{ director: "Martin Scorsese" }];
